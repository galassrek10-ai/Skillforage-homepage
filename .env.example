# Copy this file to .env and fill values. Do NOT commit real secrets.
# Backend (Express proxy) - required for AI generation
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash-preview-09-2025

# Optional: Firebase web config as a JSON string (if you use Firebase)
# Example: FIREBASE_CONFIG='{"apiKey":"...","authDomain":"...","projectId":"..."}'
FIREBASE_CONFIG='{}'
